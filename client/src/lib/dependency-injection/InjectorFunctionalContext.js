import { createContext } from 'react';

// This class should be called InjectorContext - though there's already a file with the name
// injectorContext that should be renamed to injectorContextType
export default createContext({});
